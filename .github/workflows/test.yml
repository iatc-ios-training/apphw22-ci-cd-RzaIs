name: Build and Test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: testing
    runs-on: macos-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3
      
      - name: build and test
        run: |
          bundle exec fastlane test